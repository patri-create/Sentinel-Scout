# Sentinel & Scout ML - Real-Time Fraud Detection
Machine Learning Engineering project oriented to production.

## How to run:
1. Install `uv`
2. Run `uv run uvicorn app.main:app --reload`
3. See docs at `http://localhost:8000/docs`


## How to test:
1. Install `pytest`
2. Run `uv run pytest -v`